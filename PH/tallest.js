// # find the max number in an Array 
const height = [68, 67, 56, 78, 90, 50];

function getMax(numbers) {
    for(const num of numbers) {
        let max = num;
        console.log(num);
        
    }
}

const max = getMax(num);
console.log("max value is : ", max);
